import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const StyledPartyBox = styled(Box)({
  textAlign: "center",
  marginTop: 80,
});
export const StyledPartyBoxItems = styled(Box)({
  display: "flex",
  flexWrap: "wrap",
  marginTop: 16,
  justifyContent: "center",
  gap: "30px",
});
export const StyledPartyBoxText = styled(Box)({
  fontWeight: "bolder",
  fontSize: 32,
});
